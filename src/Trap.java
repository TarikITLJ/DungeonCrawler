import java.awt.*;

public class Trap extends SolidSprite {
    /**
     * Initialise un piège avec une image et des dimensions.
     * @param image Image du piège
     * @param x Position horizontale sur l'écran
     * @param y Position verticale sur l'écran
     * @param width Largeur du piège
     * @param height Hauteur du piège
     */
    public Trap(Image image, double x, double y, double width, double height) {

        super(image, x, y, width, height);


    }
}
